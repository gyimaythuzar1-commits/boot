# مكتبات بوت التليجرام
pyTelegramBotAPI==4.16.1

# مكتبات التعامل مع الإنترنت والطلبات
requests==2.31.0
urllib3==2.2.1
httpx==0.28.1
certifi>=2020.6.20
idna>=2.10

# مكتبات معالجة البيانات والنصوص
regex==2023.12.25

# مكتبات المساعدة (موجودة في بايثون بس بنكتبها لضمان وجودها)
uuid==1.30
typing-extensions>=4.5.0

# مكتبة إدارة قاعدة البيانات SQLite (اختيارية)
sqlite3-binary

# مكتبات الثريدينج واللوجينغ (مدمجة بالفعل لكن بنضمنها)
thread6==1.0.1
loguru==0.7.2